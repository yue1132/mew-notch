name: Build and Release

on:
  release:
    types: [published]

jobs:
  build:
    name: Build and sign app
    permissions:
      contents: write
    runs-on: macos-26
    environment: main
    env:
      projname: "MewNotch"

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Resolve Swift packages
        run: xcodebuild -resolvePackageDependencies -project ${{ env.projname }}.xcodeproj

      - name: Install Apple certificate
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          CERT_PATH=$RUNNER_TEMP/build_certificate.p12
          KC=$RUNNER_TEMP/app-signing.keychain-db
          echo -n "$BUILD_CERTIFICATE_BASE64" | base64 --decode > "$CERT_PATH"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KC"
          security set-keychain-settings -lut 21600 "$KC"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KC"
          security import "$CERT_PATH" -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k "$KC"
          security list-keychain -d user -s "$KC"
          security find-identity -v -p codesigning

      - name: Build with Xcode 26
        run: xcodebuild -version

      - name: Build and archive
        run: |
          xcodebuild clean archive \
            -project ${{ env.projname }}.xcodeproj \
            -scheme ${{ env.projname }} \
            -archivePath ${{ env.projname }} \
            -destination "generic/platform=macOS" \
            ONLY_ACTIVE_ARCH=NO \
            -allowProvisioningUpdates

      - name: Export app
        run: |
          xcodebuild -exportArchive -archivePath "${{ env.projname }}.xcarchive" -exportPath Release -exportOptionsPlist ".github/exportOptions.plist"

      - name: Create DMG
        run: |
          create-dmg \
            --volname "MewNotch Installer" \
            --volicon "MewNotch/Resources/Assets/MewAssets.xcassets/AppIcon.appiconset/MewNotch-1024.png" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --background ".github/res/Installer-Background.png" \
            --icon-size 128 \
            --icon "MewNotch.app" 200 185 \
            --hide-extension "MewNotch.app" \
            --app-drop-link 600 185 \
            "MewNotch-${{ github.ref_name }}.dmg" \
            "Release/MewNotch.app"

      - name: Generate Sparkle Appcast
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: |
          chmod +x .github/generate_appcast
          
          mkdir -p ReleaseDist
          cp "MewNotch-${{ github.ref_name }}.dmg" ReleaseDist/
          
          echo "$SPARKLE_PRIVATE_KEY" > sparkle_key.pem
          
          DOWNLOAD_URL="https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/"
          
          .github/generate_appcast \
            --ed-key-file sparkle_key.pem \
            --download-url-prefix "$DOWNLOAD_URL" \
            ReleaseDist/
            
          rm sparkle_key.pem

      - name: VirusTotal Scan
        uses: cssnr/virustotal-action@v1
        with:
          vt_api_key: ${{ secrets.VT_API_KEY }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          file_globs: |
            MewNotch-${{ github.ref_name }}.dmg
          update_release: true
          release_heading: '### VirusTotal Analysis'
          
      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./MewNotch-${{ github.ref_name }}.dmg
          asset_name: MewNotch-${{ github.ref_name }}.dmg
          asset_content_type: application/x-apple-diskimage
          
      - name: Publish Appcast to GitHub Pages
        run: |
          cp ReleaseDist/appcast.xml /tmp/appcast.xml

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git fetch origin sparkle-pages
          git checkout sparkle-pages

          mkdir -p sparkle

          mv /tmp/appcast.xml sparkle/appcast.xml

          git add sparkle/appcast.xml

          git commit -m "Update Sparkle appcast for ${{ github.ref_name }}"
          git push origin sparkle-pages
